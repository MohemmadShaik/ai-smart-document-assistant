spring:
  main:
    web-application-type: servlet
  application:
    name: ai-smart-document-assistant

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini

server:
  port: 8081

